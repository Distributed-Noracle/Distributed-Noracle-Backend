apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: noracle-pvc
  namespace: ma-breuer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: nfs-client
  volumeMode: Filesystem
